export type TaskData = {
	title: string;
	description: string;
	result: string;
	dateEnd: string;
};

export type GoalData = {
	title: string;
	description: string;
	expectedResult: string;
	dateEnd: string;
};
